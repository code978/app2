<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
    <title>app2 --> app1</title>
</head>

<body class="bg-info">
    <h1 class="text-center mt-3">App2 :🎁🎁</h1>
    <form action="/gettime" method="GET">
        <h1 class="text-center mt-2"><button class="btn btn-warning">Click me🎧.</button></h1>
    </form>

    <!-- for showing the table -->
    <div class="container">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th class="text-center">S.no.</th>
                    <th class="text-center">Time</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i < time.length; i++) { %>
                <tr>
                    <td><%= i %></td>
                    <td><%= time[i].response %></td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    
</body>

</html>